{
  "name": "feishu-mail",
  "version": "0.3.0",
  "description": "飞书官方邮件插件 —— 用户登录飞书后，数字员工可读写其飞书邮箱：查/搜/读邮件与附件链接、标已读打标签、发送/回复/转发。",
  "mcpServers": {
    "feishu": {
      "type": "stdio",
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/dist/feishu-mail.mjs",
        "serve"
      ],
      "env": {
        "LARK_APP_ID": "${LARK_APP_ID}",
        "LARK_APP_SECRET": "${LARK_APP_SECRET}",
        "FEISHU_AUTH_DIR": "${FEISHU_AUTH_DIR}"
      }
    }
  },
  "connectorLogin": {
    "feishu": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/dist/feishu-mail.mjs",
        "login"
      ],
      "env": {
        "LARK_APP_ID": "${LARK_APP_ID}",
        "LARK_APP_SECRET": "${LARK_APP_SECRET}",
        "FEISHU_AUTH_DIR": "${FEISHU_AUTH_DIR}"
      },
      "loggedInProbe": [
        "${CLAUDE_PLUGIN_ROOT}/dist/feishu-mail.mjs",
        "status"
      ]
    }
  }
}
